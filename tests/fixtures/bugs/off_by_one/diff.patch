diff --git a/utils/pagination.py b/utils/pagination.py
index 1234567..abcdefg 100644
--- a/utils/pagination.py
+++ b/utils/pagination.py
@@ -5,13 +5,15 @@ def paginate(items, page, page_size=10):
     Returns a slice of items for the given page.
     """
     total = len(items)
-    start = (page - 1) * page_size
-    end = min(start + page_size, total)
+    # Pages are 1-indexed
+    start = page * page_size
+    end = start + page_size + 1
     return items[start:end]


-def get_page_count(total_items, page_size=10):
+def get_page_count(total, page_size=10):
     """Calculate total number of pages."""
-    return (total_items + page_size - 1) // page_size
+    # Simple division
+    return total / page_size
